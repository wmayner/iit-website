.iit-header
  margin 40px 0
  .jumbotron
    padding 20px
    + h1
      margin-top 0
  .title
    display inline-block
    vertical-align middle
    width 75%
    margin-left 25px
    h1
      font-family 'Varela Round'
      text-transform uppercase
      line-height 1.3
      letter-spacing 0.1ex
      word-spacing 0.3ex
      font-size 125%
    p
      font-size 70%
      font-weight 300
      line-height 1.5
  .logo
    display inline-block
    width 15%
    max-width 110px
    vertical-align middle
    text-align center
    padding 0
    margin 10px
    img
      max-width 100%
      max-height 100px

// xs
unquote("@media (max-width: "+xs_max+") {")
.iit-header
  margin 20px 0
  .jumbotron
    .logo
      width 17%
      margin 0
      max-width 80px
      img
        max-height 70px
    .title
      width 65%
      @media (min-width: 568px)
        width 75%
      margin-left 15px
      h1
        margin 5px 0 0 0
        font-size 80%
        @media (min-width: 568px)
          font-size 90%
unquote("}")

// sm
unquote("@media (min-width: "+sm_min+") and (max-width: "+sm_max+") {")
.iit-header
  .title
    h1
      font-size 100%
    p
      80%
unquote("}")

// md
unquote("@media (min-width: "+md_min+") and (max-width: "+md_max+") {")
.iit-header
  .title
    h1
      font-size 110%
    p
      80%
unquote("}")

// lg
unquote("@media (min-width: "+lg_min+") {")
unquote("}")

// md, lg
unquote("@media (min-width: "+md_min+") {")
unquote("}")
